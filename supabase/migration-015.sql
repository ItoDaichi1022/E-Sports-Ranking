-- ============================================================================
-- 差分適用スクリプト 015
--
-- 既に schema.sql（＋002〜014）を実行済みのプロジェクトに、あとから入った
-- 変更だけを当てる。Supabaseダッシュボードの SQL Editor に貼り付けて「Run」。
-- 何度実行しても同じ結果になる。
--
-- 新しくプロジェクトを作る場合はこれは不要（schema.sql に取り込み済み）。
--
-- 内容:
--   大会の配信元（配信を見に行くURL）。運営が大会情報に記載し、
--   選手・観戦者は大会の詳細ページからそのまま配信を開ける。
--
--   名前は入れず、URLだけを持つ。表示名を別に持たせると運営の入力が増えるうえ、
--   リンク先と食い違った名前を書けてしまう（画面ではURLのドメインを見出しに使う）。
-- ============================================================================

alter table tournaments
  add column if not exists stream_url text;
